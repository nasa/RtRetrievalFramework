# Just testing that various ILS modes work so do not need to simulate
# on full levels, use retrieval levels
include = <CONTROL_FILE_DIRECTORY>/simulate_ret_levels.config

# Include list of ILS types to try
include = <CONTROL_FILE_DIRECTORY>/ils_testing.inc
